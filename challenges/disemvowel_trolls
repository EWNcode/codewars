def disemvowel(string):
    vowels = 'aAeEiIoOuU'
    string = ''.join([ch for ch in string if ch not in vowels])
    return string


test.assert_equals(disemvowel("This website is for losers LOL!"),
                              "Ths wbst s fr lsrs LL!")
